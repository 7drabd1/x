# Local Quran audio

Place a licensed Quran recitation video at `public/audio/quran.mp4`. The main tasbeeh screen has one play button that loads this local video with a hidden HTML `<video>` element, so visitors hear only the audio without seeing the video. The interface remains usable and displays a helpful message until the file is supplied.
