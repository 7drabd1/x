# Local Quran audio

Place a licensed Quran recitation MP3 at `public/audio/sound.mp3`. Quran cards expose a play button that loads this local file with a hidden HTML `<audio>` element. The interface remains usable and displays a helpful message until the file is supplied.
